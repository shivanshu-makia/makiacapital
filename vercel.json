{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && NODE_OPTIONS=--openssl-legacy-provider npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo skip",
  "framework": null
}
